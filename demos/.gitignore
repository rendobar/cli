# MP4 + WebM are heavy and regenerated by CI on every release.
# GIFs are tracked so the README + docs can hotlink them.
out/*.mp4
out/*.webm

# Local preview artefacts (build-preview.mjs output)
preview.html
preview-*.jpeg
